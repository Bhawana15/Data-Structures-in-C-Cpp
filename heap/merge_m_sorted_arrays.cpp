#include <iostream>
#include <vector>
#include <queue>
using namespace std;

void merge_m_sorted (vector<int>& list[]) {
	int m = list.size();
	vector<int> v;
	while ()
}

int main()
{
    // M arrays of variable size
    vector<int> list[] =
    {
        { 10, 20, 30, 40 },
        { 15, 25, 35 },
        { 27, 29, 37, 48, 93 },
        { 32, 33 }
    };
 
    int M = sizeof(list) / sizeof(list[0]);
 
    merge_m_sorted(list, M);
 
    return 0;
}